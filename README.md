# Spring Dependency Injection Example (Java-based Configuration)


A simple Spring application demonstrating Dependency Injection (DI) using Java-based configuration instead of XML.  
The Student class depends on the Course class, and Spring injects the dependency using @Configuration and @Bean annotations
